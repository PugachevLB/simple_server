apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "typicalapp.fullname" . }}-test-connection"
  labels:
    {{- include "typicalapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "typicalapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
