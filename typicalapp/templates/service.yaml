apiVersion: v1
kind: Service
metadata:
  name: {{ include "typicalapp.fullname" . }}
  labels:
    {{- include "typicalapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "typicalapp.selectorLabels" . | nindent 4 }}
